#!/bin/sh

IP=$(tr '[:upper:]' '[:lower:]' < "/etc/tinc/$NETNAME/hosts/$NAME" | grep ^subnet | sed -e 's/ //g' -re 's/^(subnet|subnet=)//' | head -n1)

/usr/bin/env ip link set "$INTERFACE" up
/usr/bin/env ip addr add "$IP" dev "$INTERFACE"
